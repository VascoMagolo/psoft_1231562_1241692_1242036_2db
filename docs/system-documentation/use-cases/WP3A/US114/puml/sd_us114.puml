@startuml
autonumber
actor "ATCC" as Actor
participant "RouteController" as Ctrl
participant "SearchRoutesUseCase" as UC
participant "RouteRepository" as Repo
database "Database" as DB

Actor -> Ctrl : GET /api/routes/search?origin=...&destination=...&page=0&size=20
activate Ctrl

Ctrl -> UC : execute(origin, destination, pageable)
activate UC

alt Origin and destination provided
    UC -> Repo : findByOriginAndDestination(origin, destination)
    activate Repo
    Repo -> DB : SELECT FROM route WHERE origin = ? AND destination = ?
    activate DB
    DB --> Repo : route records
    deactivate DB
    Repo --> UC : Page<Route>
    deactivate Repo
else Only origin provided
    UC -> Repo : findByOrigin(origin)
    activate Repo
    Repo -> DB : SELECT FROM route WHERE origin = ?
    activate DB
    DB --> Repo : route records
    deactivate DB
    Repo --> UC : Page<Route>
    deactivate Repo
else Only destination provided
    UC -> Repo : findByDestination(destination)
    activate Repo
    Repo -> DB : SELECT FROM route WHERE destination = ?
    activate DB
    DB --> Repo : route records
    deactivate DB
    Repo --> UC : Page<Route>
    deactivate Repo
else No filters provided
    UC -> Repo : findAll()
    activate Repo
    Repo -> DB : SELECT FROM route
    activate DB
    DB --> Repo : route records
    deactivate DB
    Repo --> UC : Page<Route>
    deactivate Repo
end

UC --> Ctrl : Page<Route>
Ctrl --> Actor : 200 OK (HAL+JSON with _embedded, _links, page)

deactivate UC
deactivate Ctrl
@enduml
